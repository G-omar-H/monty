#include "monty.h"
/**
 * stack - set the stack data_structur's type.
 * @stack: top stack tracker
 * @line_number: line number track
 * Return: void.
 */
void stack(stack_t **stack, unsigned int line_number)
{
	(void)line_number;
	(void)(*stack);
	arg->mode = 0;
}
/**
 * queue - set the queue data_struture's type.
 * @stack: top stack tracker
 * @line_number: line number track
 * Return: void.
 */
void queue(stack_t **stack, unsigned int line_number)
{
	(void)line_number;
	(void)(*stack);
	arg->mode = 1;
}
